#!/usr/bin/env bash
#
# Wrapper script to invoke the phrase replacement script
#
# Copyright (c) 2026 Paul Sinclair
# SPDX-License-Identifier: MIT
# For license details, see LICENSE in the repository root.
#
# Usage:
#   scripts/bin/replacephrases [configfile] [--dry-run]
#
# Examples:
#   scripts/bin/replacephrases                           # Uses default config
#   scripts/bin/replacephrases config/phrase_replacements.txt
#   scripts/bin/replacephrases config/custom.txt --dry-run
#

# Get the directory where this script is located
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Invoke the Python script with all arguments
python3 "$SCRIPT_DIR/../helpers/replace_phrases.py" "$@"
