# CODEOWNERS

## Copyright (c) 2026 Paul Sinclair  
## SPDX-License-Identifier: MIT  
### For license details, see `<repo>/LICENSE`.

## See `<repo>/README.md` for an introduction to briteTest.

## See '\<repo\>/config/contributors.md for a list of contibutors and their roles (contributor-only), reviewer, or approver.


## Owners for the entire repository:
* @paulsinclair51

## Owners for subsets of the repository:
### Currently, none.

# Entries:
#   <owns> @<owner> [ @<owner>]...
#
# <owns>: pattern defining a wildcard path. Pattern quick reference:
#   *            = any chars except '/' (single path segment).
#   **           = any chars including '/' (cross-directory).
#   ?            = exactly one char except '/'.
#   /foo/bar     = path anchored at repository root.
#   docs/*.md    = .md files directly under a docs directory relative
#                  to where matched (not recursive).
#   /docs/*.md   = .md files directly under root /docs/.
#   /docs/**     = everything under /docs/, recursively.
#
#   Note: character classes like [a-z] are not supported in patterns.
#
# <owner>: GitHub username (account handle/login) or team <org>/<team>.
#
# If patterns overlap (or are duplicated) in CODEOWNERS, the last matching
# pattern wins. So,
#   - Overlap: multiple patterns match same file: GitHub uses the owner(s)
#     from the bottom-most matching rule.
#   - Duplicate same pattern: later duplicate overrides earlier one.
#   - Multiple owners on one line: all owners on that winning line are
#     requested.
#
# Examples:
#   /docs/ @marcusjones
#   /.github/workflows/ @maryjames @britetest/docs-team
#
# Best practices:
#   - CODEOWNERS for this repository is kept at <repo>/.github/CODEOWNERS.
#   - Paths are case-sensitive. Match actual file/directory casing exactly.
#   - Place broad rules first and more specific rules later (last match wins).
#   - Prefer teams for shared ownership (e.g., @org/team) to avoid single-person
#     bottlenecks.
#   - Team owners must be visible and have write access to the repository,
#     or review requests may not work as expected.
#   - Use comments to clarify ownership intent, but keep active rules minimal
#     and unambiguous.
#   - Owners are requested for changed files in pull requests (PRs); ownership does
#     not by itself block merges unless branch protection/rulesets require approvals
#     and code-owner review. Branch protection/rulesets are in effect for this
#     repository.
#
# References:
#
#   1. '<repo>/.github/workflows/branch-name-validation.yml' for the GitHub
#      branch name validation workflow.
#   
#   2. '<repo>/scripts/bin/setup_rulesets' for the script used to set up or
#      redefine the repository rulesets.
#
#   3. '<repo>/docs/md/Contributor_Guide.md' for more information about
#      contributors, reviewers, approvers, and guidance of source control
#      management (SCM) for the repository.

lsbranch dirty smoke test

lsbranch dirty smoke test
