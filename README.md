# utest
An application programming interface (API) for use by a unit test runner.

## Project structure

This repository uses a conventional C project layout:

```text
include/  Public header files
src/      Application and library source files
test/     Test sources and fixtures
build/    Local build output (ignored except for .gitignore)
reports/  Generated test and analysis reports (ignored except for .gitignore)
```
